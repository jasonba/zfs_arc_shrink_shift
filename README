This is a program that will advise upon the correct value to set for the /etc/system
based tunable 'zfs_arc_shrink_shift'

To compile, run:

$ make zfs_arc_shrink_shift

- or -

$ make

To clean up use:

$ make clean
